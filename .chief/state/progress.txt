# Chief Progress — 900-dead-code-paydown
Started: Thu Sep  3 01:18:28 CDT 2026
---

## Codebase Patterns

- **koine has almost no code.** 140 tracked files: 65 json, 57 md, 7 tsv, 5 mjs, 2 sh.
  No `package.json`, no dependencies, no runtime (ADR-0001: "koine specifies, agora
  implements"). The entire executable surface is `scripts/check-*.mjs` (5 guards) plus
  `.chief/verify.sh` + `.chief/verify-test.sh`. A "dead code" sweep here is a sweep of
  seven files.
- **`.chief/verify.sh` is the one gate, and it is path-selective.** Run it as
  `.chief/verify.sh --run <changed paths...>` — a bare run diffs `main...HEAD`, so it
  reports "no changed files" on an uncommitted working tree. `--plan` prints the
  selected gates without running them. A path matching NO rule selects the **umbrella**
  (every gate, including the self-test).
- **`check-doc-integrity.mjs`'s slugify is NOT GitHub's.** It collapses `\s+` to a
  SINGLE hyphen, so `## Class A — dead: removal candidates` anchors as
  `#class-a-dead-removal-candidates`, not GitHub's `#class-a--dead-removal-candidates`.
  Writing the GitHub form fails the gate. Match the guard.
- **`MD_LINK` fires inside backticks.** Writing `` `[text](path)` `` in prose is parsed
  as a link and reported as "no such file — path". Split the form when writing about it.
- **`tasks/chief/completed/` is history and must not be rewritten** — both doc guards
  skip it as a citer, and reference counts that include it measure the past.

---

## 2026-09-03 — US-1: An inventory, measured before anything is removed

**Done.** Wrote `docs/reference/dead-code-inventory.md` (the checked-in artifact a human
approves before deletion) and linked it from `docs/README.md`'s Reference section — that
file's own rule is *a document not linked here does not exist*.

Files changed: `docs/reference/dead-code-inventory.md` (new), `docs/README.md` (+1 line),
`.chief/state/prd.json`, `tasks/chief/900-dead-code-paydown.json`.

**Gate:** `.chief/verify.sh --run docs/reference/dead-code-inventory.md docs/README.md`
→ `guard-doc-integrity` OK (57 markdown files, 1399 relative links, 0 errors, 3 status
tables match their headers), `guard-doc-links` "0 dead vs 0 on main" → **all selected
gates green**. `check-tasklist-categories` OK, 4 tasklists categorized.

### What the inventory found, and what US-2 inherits

**Class A — 4 removal candidates, ALL inside `scripts/check-doc-links.mjs`.** That guard
was carried in from a sibling repo (`87c9aa9`) with its host's exceptions attached; its
own comments name `cuneiform`, `argos`, `studio-os`, `insimul`, `engine/quality.sh`, none
of which exist here. **The guard itself is LIVE and must stay** — `.chief/verify.sh`
selects it as `guard-doc-links` and it is the only thing checking the 8 bare `docs/…`
citations in non-Markdown files. What is dead is *inside* it:

- **A-1** `EXCEPT_DIRS` (lines 42-53, used at 81) — reads `docs/.structure-exceptions`.
  `git log --oneline --all --` → **0 commits**. Never existed on any branch.
- **A-2** `IGNORE` (lines 55-60, used at 80) — reads `docs/.linkignore`. **0 commits.**
  Also contradicts `check-doc-integrity.mjs:13-14`, which states there is deliberately no
  allowlist for a private-repo link.
- **A-3** the `docs/archive/` skip (lines 70-75) — **0 commits**, and `git grep 'docs/archive'
  -- ':!scripts/'` finds no convention, README or ADR. Flagged with a judgement note: this
  is the one whose deletion would matter if the convention were later adopted.
- **A-4** the cross-repo `foreign` walk (lines 98-117, used at 119) —
  `git grep -lE '"(repo|owner)"\s*:' -- '*.json'` → **0 of 65** tracked JSON files. `foreign`
  is always empty. Worse than inert: it runs `execFileSync('git', ['rev-parse',
  '--show-toplevel'])` **once per JSON file** (65 spawns/run) for a value that is *wrong in
  a worktree* (returns `900-dead-code-paydown`, not `koine`) and then never compared.

**Class B — 1 duplicated implementation, recorded not deleted.** Relative Markdown link
resolution exists in both doc guards. Measured: `check-doc-integrity` reaches 56 md files /
1384 links (before this story) incl. `#anchor` targets and the status mirrors;
`check-doc-links` reaches 57 / 1371, no anchors. **1371 of 1371** of the latter's md-link
checks are duplicated, more strictly, by the former. Its unique value is 8 bare `docs/…`
citations in `.chief/verify-test.sh` + `tasks/chief/91-*.json` + `tasks/chief/92-*.json`.
Left in place deliberately: `guard-doc-links` is a **ratchet** and `guard-doc-integrity` is
a **wall**, so merging them changes the merge gate's failure semantics — a behaviour change,
not a removal. If ever taken, the answer is fold `BARE_DOC` into `check-doc-integrity` and
retire `check-doc-links` whole. Also B-2: the 4th status mirror (CLAUDE.md prose) that no
guard checks — recorded so a sweep does not "simplify" it away.

**Class C — 5 survivors, each with the reason.** 8 registry rows with 0 in-repo citations
(`descended_from`, `caused_by`, `cine:scene_of`, `media:mentions`, `soc:employed_by`,
`soc:resides_in`, `…model+coreml`, `…model+tflite`) — published vocabulary vendored
downstream, and a relation's signature is immutable once published, so removal is not an
available move. The `tasks/chief/completed/` skip — structurally identical to A-3, opposite
verdict, because the directory exists; hides 0 dead citations today (replayed `BARE_DOC`
over all 45 records) so it is *deliberately unexercised*, not dead. `--json`/`--strict`/
`--list` — settable by hand, so not "flags that can no longer be set". `SKIP_DIR`/`SKIP_EXT`.
The schema fixtures (exercised by `check-schemas.mjs`).

**9 empty searches recorded** so they are not repeated: 0 orphan `.md` of 57, 0 orphan
schemas, 0 unreferenced `$defs` of 10 (80 `$ref`s all resolve), 0 real duplicated
subschemas, 0 orphan entity types / enum tokens, 0 commented-out code blocks, no
`package.json`, 0 dead branches in `verify.sh`, 0 unrun guard scripts.

### Learnings for future iterations

- **The whole Class-A set is one file and one commit's worth of work.** US-2's "reviewable
  steps rather than one sweep" is satisfiable as A-1+A-2 (the two never-existed exception
  files), then A-3, then A-4 — three commits, each re-running the gate. A-4 is the only one
  that changes runtime behaviour (removes 65 subprocess spawns per run).
- **Before deleting anything in `check-doc-links.mjs`, confirm the guard still reports
  `0 dead vs 0 on main`.** The ratchet's base comparison spawns a temporary worktree of
  `main` and runs the OLD script there, so a removal is compared against the pre-removal
  behaviour — that is exactly the right test and it is free.
- **US-3's undecidability register is already seeded**: the inventory's closing paragraph
  states the method's limit (static, scoped to this tree; koine exists to be consumed from
  outside it) and C-1's registry rows are the concrete case. US-3 should extend that, not
  re-derive it.
- The research map for this tasklist was accurate as far as it went; nothing in it needed
  correcting.
