# Chief Progress — 84-record-the-downstream-results
Started: Wed Aug 26 11:04:48 CDT 2026
---

## Codebase Patterns
- **Verify with:** `node scripts/check-doc-integrity.mjs` (links + 3 status-table mirrors; a wall)
  and `node scripts/check-doc-links.mjs --ratchet --base main` (ratchet). `.chief/verify.sh --plan`
  shows which gates a changed-path set selects; any `*.md` selects both doc gates.
- **Anchor slugs** (check-doc-integrity `slugify`, scripts/check-doc-integrity.mjs:65): strip
  backticks/`*`/`_`, drop link syntax, lowercase, delete every non-`[\w\s-]` char (so em dashes and
  apostrophes vanish), then collapse whitespace runs to ONE hyphen. So
  `## Findings — from the downstream run` → `#findings-from-the-downstream-run` (single hyphen, not
  double) and `## Downstream results — where a real run's result lands` →
  `#downstream-results-where-a-real-runs-result-lands`. Getting this wrong is a silent dead link
  that the integrity gate does catch — run it.
- **The downstream repo is `~/Development/agora`**, a sibling checkout, NOT under the worktree.
  Read-only from here (ADR-0001: agora runs, koine records). Key artifacts:
  `console/evidence/kcs-live-run.json` (the committed run), `console/src/kcs/scenarios/` (the nine
  encodings + their gates), `console/src/kcs/assertions.ts` (the §5 predicate vocabulary),
  `schemas/src/versions.ts` (`SPEC_VERSIONS` — what spec versions the run was pinned at).
- **Reading an encoding's real content** beats grepping it. From
  `~/Development/agora/console/src/kcs/scenarios`:
  `node --experimental-strip-types -e "import('./index.ts').then(m=>{...})"` works and gives you
  every step/assertion/predicate/title. That is how the per-scenario prose in this branch was
  written without guessing.

---

## US-1 — Each scenario records what its downstream run actually did (2026-08-26)

**Done.** Commit `8375dd2`.

Files changed (all under `scenarios/`): all ten `*.md` gained a `## Downstream results` section, and
`README.md` gained a `### The run of record — 2026-08-24` + `### Findings from the run — DR-1…DR-11`
pair at the end of its existing *Downstream results* section.

Source of every claim: `agora/console/evidence/kcs-live-run.json` (`evidence_id`
`sha256-2d9e6c43…c17bb3`, `generated_at` 2026-08-24T22:00:37Z, `kcs_version` 0.3.0, nine scenarios,
19/32 participant slots live = 59%, `green: true`, `live_pass: false`, verdict `partial-live`,
`transport_failures: []`) plus the encodings themselves, plus koine's own
`docs/reference/kcs-encoding-gate-verification.md`.

**Findings opened — DR-1…DR-11.** New id namespace (no collision with A–E / F–L / FT-* / V-* / MA-* /
M-1 / KGP-* / INT-11). Each is defined ONCE, in the document it bites; `scenarios/README.md` holds
only an index. The load-bearing ones:
- **DR-4** — `kcs:kmi-otio-roundtrip` is `kcs:media-transform` spread into a new object with a new
  `id`/`title` and the SAME fixture. It asserts nothing OTIO-specific, so M-1 and the KMI 0.3.3 fold
  are unexercised: KMI's artefact gate is met by count, not by content.
- **DR-10** — the downstream §5 vocabulary has drifted from KCS §5 **both ways**: no
  `structure_matches` (KCS 0.3.0 delta Q) and an extra `media_map_complete` that koine names
  nowhere. The check meant to catch it is `expect(ASSERTION_NAMES).toHaveLength(18)` — a hardcoded
  count, and 18 is coincidentally what both lists hold. Every document declares `kcs_version: 0.3.0`
  while being replayed by a 0.2.0-shaped vocabulary.
- **DR-5** — the suite pins **KFT 0.5.0** (`SPEC_VERSIONS.kft`), so §3.3's conversion round-trip and
  §8.1's graded refusals have no encoded assertion at all; every refusal is the ungraded `refused`.
- **DR-11** — `kft-resume-checkpoint.md` (KFT 0.6.0) has no encoding and no run.
- **DR-7 / DR-8** — the reading hazard: `kcs:live-schema-mutation` and `kcs:multi-authority` both come
  back `green` over passes with four and six blocking deltas open, because the encodings deliberately
  do not assert an unfolded delta.

**Learnings for future iterations**
- The PRD's premise ("nine scenarios have an empty `## Downstream results` section") was **wrong in
  the reader's favour**: there were no such sections at all, and there are **ten** scenarios, not
  nine. `kft-resume-checkpoint.md` landed after the encodings were built. Do not assume the count.
- `green` in the evidence artifact means *every encoded step and assertion passed with no transport
  failure* — it does NOT mean the pressure test came out clean. Two of nine green scenarios have
  blocking deltas open. Any prose written off that field must say so or it repeats the `passes: true`
  mistake this portfolio already audited.
- The evidence artifact records **per-scenario aggregates only** — no per-assertion result, no
  clause field (KCS defines none). So every clause named in the new sections was read by hand off the
  encoding. That is DR-2, and it is a KCS re-open input, not a defect in the run.
- Remaining for this tasklist: **US-2** (the `⬜ planned` column in `scenarios/README.md` — note the
  stale-warning block at its § *The KCS encodings* explicitly defers the flip to this tasklist, and
  the sentence *"it says `planned` eight times and `exists` zero times"* is itself wrong twice over:
  it is nine `planned` rows and there are ten scenarios; plus `ROADMAP.md`'s restatement) and
  **US-3** (feed DR-3/DR-5/DR-10/DR-11 into the specs' open questions or named gates, and record the
  positive evidence; coordinate with `83-promote-kgp` on KGP rather than promoting from here — note
  DR-3 says this run does NOT discharge KGP's fixture gate).

---

## US-2 — The KCS-encoding column stops lying, in both files (2026-08-26)

**Done.** Commit `efeebed`.

Files changed: `scenarios/README.md`, `ROADMAP.md`.

**`scenarios/README.md`.** The **KCS encoding** column went from `⬜ planned` ×9 (plus two `focused
pressure leg` cells) to `✅ exists` ×9 + `⬜ absent` ×1. Every cell now names three checkable things:
the scenario id (`kcs:…`), the encoding file under `agora/console/src/kcs/scenarios/`
(`worlds-to-fabric.ts`, `media-transform.ts`, `kmi-otio-roundtrip.ts`, `finetune.ts`,
`finetune-multimodal.ts`, `producer-exhaust-finetune.ts`, `live-schema-mutation.ts`,
`multi-authority.ts`, `format-stress.ts` — read off `index.ts`'s `KOINE_SCENARIOS`, whose `source`
field is the koine-relative path, so the mapping is the downstream repo's own and not inferred), and
a `→ [results](…#downstream-results)` link into the section US-1 wrote. Where a DR finding qualifies
what the run proved, the cell carries it: DR-4 (⚠️ `kcs:kmi-otio-roundtrip`), DR-5, DR-6, DR-7, DR-8,
DR-9, DR-10, DR-11.

The stale-warning block that deferred this flip is **replaced** by a dated *Corrected* note (the
deferral is discharged, so leaving the warning would be the next stale thing). The three state
definitions stay three, with two additions: `exists` gains the ⚠️ qualifier (*count met, content
not*), and `planned` is recorded as reading **zero** times today but kept because the next scenario
added starts there. A new *Why the tenth row is `absent` and not `planned`* paragraph carries the
`coverage.test.ts` set-equality consequence that used to live in the warning block.

**AC-4's note** is the *These two registers must agree, and this one is the register of record*
paragraph: `scenarios/README.md` is the register, `ROADMAP.md` Phase F4 restates it and is the bug on
disagreement (same rule as the spec version/status mirrors) — but **neither is the source**. Both
derive from `agora/console/src/kcs/scenarios/` + `console/evidence/kcs-live-run.json`, so a
disagreement with those closes by re-verifying against them, never by editing prose to match a
memory. Kept README-level: CLAUDE.md forbids a pointer to the downstream repo in any spec, schema,
registry, or policy file.

**`ROADMAP.md`.** Three restatements corrected, all of which had propagated the false column:
- Phase F4's two milestone rows — "the `scenarios/README.md` column has not been flipped yet (`84`)"
  and "the run happened, the recording did not (`84`)"; the encode row now also lists the nine
  encoding filenames and names the tenth scenario as unencoded (DR-11).
- Phase F4's bullet "Nothing was recorded back into koine. All nine `## Downstream results` sections
  are still empty" → ✅ closed 2026-08-26, with the DR-1…DR-11 index link and the register-of-record
  rule restated.
- Phase 1's *Depended on* caveat ("all nine of those sections are empty") → closed, with the
  reader's warning that DR-7/DR-8 sit under green scenarios and DR-3 keeps KGP's fixture gate open.

Two other stale bits fixed in passing, both in the same table/section: the intro's "These **eight**
documents" → ten, and `kcs-format-stress.md`'s *What it hunts* cell saying it encodes "the **two**
hand-written scenarios above".

**Gates:** `node scripts/check-doc-integrity.mjs` → **OK**, 47 markdown files, **918** relative links,
all 3 status tables match their headers. `node scripts/check-doc-links.mjs --ratchet --base main` →
**0 dead vs 0 on main**. `node scripts/check-tasklist-categories.mjs` → OK, 6 tasklists categorized.

**Learnings for future iterations**
- **The PRD's own numbers were wrong again, in the same direction as US-1.** It quotes ROADMAP as
  saying `planned` "eight times and `exists` zero"; the column actually read `planned` **nine** times
  plus two `focused pressure leg` cells, over **ten** rows. Count the rows, do not trust a
  restatement — that is literally the failure mode this story is about.
- **`index.ts`'s `KOINE_SCENARIOS[].source` is the authoritative koine↔encoding mapping.** It is a
  koine-relative `scenarios/*.md` path chosen precisely so the coverage gate can compare sets. Use it
  rather than matching filenames by eye — `kmi-otio-roundtrip.ts` is 696 bytes and re-exports
  `MEDIA_TRANSFORM`, which is only visible from the file, and is why that row is ⚠️ and not ✅.
- **`.chief/state/prd.json` stories may have NO `passes` key at all** rather than `passes: false`
  (US-2 and US-3 were both key-less at HEAD). Absent reads as false; add the key when flipping, and
  do not "fix" a missing key on a story you are not working.
- **Anchor gotcha, confirmed again:** `### Findings from the run — DR-1…DR-11` slugifies to
  `#findings-from-the-run-dr-1dr-11` — ONE hyphen where the em dash was, and `…` vanishes entirely.
  `check-doc-integrity.mjs` catches it; run it before committing a cross-file anchor link.
- Remaining for this tasklist: **US-3** — feed DR-3/DR-5/DR-10/DR-11 into the specs' open questions
  or named gates, and record the *positive* evidence (AC-2), which today is mostly
  `kcs:worlds-to-fabric`'s fully-live 3/3 pass. Coordinate with `83-promote-kgp` on KGP rather than
  promoting from here; DR-3 says this run does **not** discharge KGP's fixture gate.

## ⚠️ BASE INTEGRATION REQUIRED — do this FIRST, before any user story

`chief/84-record-the-downstream-results` is **8 commit(s) behind** its base `main` (@da48d29) and
chief's automatic rebase onto that base CONFLICTS. The rebase was aborted, so
your branch is exactly as it was and nothing is lost — but it cannot merge
until the base is integrated, and every commit that lands on `main` while
this is unresolved makes it worse.

**Your first task this iteration is to integrate the base yourself:**

1. `git rebase main` in this worktree.
2. At each stop, resolve the conflicts keeping BOTH sides' intent — the
   `main` side is already-merged work, so never discard it, and never
   discard your own — then `git add <files>` and `git rebase --continue`.
   `git rebase --abort` returns you to exactly this state if you need it.
3. Re-run the project's quality gates and make them green.

Rebase, not `git merge main`: chief rebases this branch again at merge time,
and a rebase drops merge commits and replays the originals — a resolution
recorded in a merge commit would be thrown away and this conflict would come
straight back. Resolving inside the rebase puts the fix in your own commits.

Then continue the normal loop: pick the highest-priority story with
`passes:false` and implement it. The integration is NOT a user story — do not
flip any story's `passes` for it, and do not report completion until the
stories themselves are really done.

## Conflicting paths
- .chief/state/prd.json
- .chief/state/progress.txt

base: main @da48d29 · behind: 8 commit(s) · branch: chief/84-record-the-downstream-results
(full note: .chief/state/INTEGRATE-BASE.md)

---

## BASE INTEGRATION (2026-08-26) — done before US-3

`main` had moved 8 commits ahead (KCB 0.4.7 / `chief/70`, which added an **eleventh** scenario
`scenarios/kcb-subscription-firehose.md`). Rebased 5 commits onto `main` @da48d29. Commit `2257748`.

**Conflict resolutions**
- `.chief/state/prd.json` + `.chief/state/progress.txt` — `git checkout --theirs` (in a rebase
  **theirs = the commit being replayed = this branch**). main's copies are tasklist `70`'s run
  state, mine are `84`'s; they are per-run files, so mine wins wholesale. Note `git add` on
  `.chief/state/*` prints "paths are ignored" and **stages anyway** (tracked files, `.gitignore`
  matches the dir); `git check-ignore` correctly reports not-ignored. Just ignore the warning.
- `scenarios/README.md` scenario table — merged both sides by script: took this branch's rewritten
  **KCS encoding** column for the ten rows, took main's new `kcb-subscription-firehose` row, and
  restyled its fifth cell into this branch's column vocabulary (`⬜ absent`).

**Then the follow-through main's new file needed** (integration, not a story): the eleventh scenario
had no `## Downstream results` section. Gave it one in the tenth's shape (never encoded, never run,
no owner) + finding **DR-12**, indexed it in `scenarios/README.md`, and corrected every count that
had gone stale — ten→eleven scenarios, `absent` once→twice, DR-1…DR-11→DR-1…DR-12, and the two
claims that KFT was the *only* spec to lose the artefact gate / the *only* red row downstream.

---

## US-3 — What the results change is followed through (2026-08-26)

**Done.** Commit `06928ab`. Tasklist complete.

**Files changed:** all six `specs/*.md`, `specs/README.md`, `docs/reference/promotability.md`,
`ROADMAP.md`, `CLAUDE.md`.

**Shape of the change.** Each spec got (a) a *Downstream evidence* block inside its existing
**Pressure test** / gate section and (b) a dated **Editorial** changelog entry. **No version, no
status, and no clause moved** — the house precedent is KMI/KCB/KINP's `Editorial (2026-08-24)`
entries, which say "No clause changes and the status does not move" in as many words. `Last updated`
bumped to 2026-08-26 on the three specs that were behind.

| Spec | Positive evidence (AC-2) | Findings folded (AC-1) |
|---|---|---|
| KINP | only fully-live cast (3/3, `live-pass`); `firewall_holds` + 2 `no_sameas_across_worlds` incl. the 4-hop path; deltas A, C | — (run predates ADR-0012, touches no federation surface; MA-1…MA-4 unmoved) |
| KGP | §3.3 `claims_converge` under machine replay; §7's two egress legs **refused** | **DR-3** — R3 unencoded, fixture gate unmoved |
| KMI | delta H/I + §5 bridge asserted over bound values | **DR-4** — artefact gate met by *count, not content* |
| KCB | delta F path planning, K ceiling, L tolerance, G refusal | **DR-7/DR-8** (green over 4 and 6 blocking deltas), **DR-12** (§4.2 unencoded) |
| KFT | the §4.2 gate **refused five times** (local-only corpus, FT-B, FT-A, FT-D, FT-F) | **DR-5**, **DR-6**, **DR-11** |
| KCS | nine documents parse + replay + report; deltas M/O/N/P exercised; §7.1's escape hatch used as designed twice (V-8, MA-11) | **DR-10** → §7.1 (new second half: a runner-side vocabulary-version obligation) + §7.2 + Pressure test; **DR-2** → §7.3 (now a *report-shape* question) |

**AC-3 (coordination) honored:** KGP is **not** promoted here. Its entry states DR-3 explicitly —
the fully-live pass does not touch the §4.1 round-trip fixture — and points promotion at
`87-kgp-projection-reader-and-roundtrip`. `83-promote-kgp` had already merged and reached the same
verdict via `docs/reference/kgp-projection-gate-verification.md`; nothing was duplicated.

**Registers reconciled** (this is where the stale claims were): `specs/README.md` said *"the artefact
gate is met for every spec in the fabric"* and *"all nine of those sections are still empty"* — both
false now, replaced with **four of six** plus the two conditions on citing the run.
`docs/reference/promotability.md`'s two *facts that apply to every row* were the same two errors; all
six table rows and the *Cheapest first* ranking now carry their DR qualifications (KCB went three
counts → four). `ROADMAP.md`'s *"No row here is blocked by F4 any more"* corrected.

**Gates observed:** `check-doc-integrity` **OK** — 48 markdown files, **1010** relative links, all 3
status tables match. `check-doc-links --ratchet --base main` → **0 dead vs 0 on main**.
`check-schemas` OK · `check-registry` OK · `check-tasklist-categories` OK (5 tasklists).
`bash .chief/verify.sh --run <10 files>` → "all selected gates green — allowing merge".

**Learnings for future iterations**

- **A `## Downstream results` section is now an obligation for every new `scenarios/*.md`,** on top
  of the KCS-encoding obligation CLAUDE.md already names. main added a scenario without one and it
  was invisible until this branch rebased. Worth adding to CLAUDE.md's scenarios bullet if a third
  instance appears.
- **The `green`-is-not-a-verdict rule is the single most reusable output of this tasklist.** It is
  now stated in `specs/README.md`'s gate, in KCB's and KMI's pressure tests, in `promotability.md`
  and in `CLAUDE.md`. Any future prose citing the downstream run must carry it or it repeats the
  `passes: true` audit.
- **Recording evidence against a spec is Editorial, not a version bump** — it moves no clause. But
  say so *explicitly* in the entry (as every prior Editorial entry does), because the reader's
  default assumption on seeing a spec diff is that something normative moved. The one judgement call
  here was KCS §7.1 gaining a second half: an open **question** is informative, so still editorial,
  and the entry names the call rather than burying it.
- **`docs/reference/promotability.md` is the per-spec register that a finding must reach** to be
  "followed through" in any useful sense — it is what a promoter reads. `ROADMAP.md` is the program
  view and `scenarios/README.md` is the encoding-state register of record. Three different jobs;
  do not collapse them.
