# Chief Progress — 89-the-re-run-three-specs-wait-on
Started: Thu Sep  3 00:43:57 CDT 2026
---

## US-1 — The four steps are walked by hand, and the verdict is per-step (2026-09-03)

Walked Steps 1–10 of `scenarios/e2e-multi-authority.md` by hand against the CURRENT folded spec
text (KINP 0.4.0 / KCB 0.5.0 / KMI 0.3.5), reading each cited section rather than replaying the
encoding. New section in the scenario: **"Re-run — Steps 1–10 walked by hand against the folded
text (2026-09-03)"**, inserted before *Re-ratification — what this pass gates*.

Per-step verdict: 1 ✅ holds · 2 ✅ flips · 3 ✅ flips · 4 ✅ flips · 5 🔴 **does not flip** ·
6 ✅ flips · 7 ✅ flips · 8 ✅ holds · 9 ✅ flips · 10 🔴 **does not flip**.

**The two blockers, both verified against the text, not inferred:**
- **Step 5** — ADR-0014 (Accepted 2026-08-26) is *decided but unwritten*: §7.3's deprecated marking
  has no field in KCB §2's manifest or §3's `find` response, and §3.1(d)'s de-dup converse keys on
  `(provider id, (name,version), schema_id)` so a stale + fresh attribution merge into one entry
  with an undefined marking, and §3.1(e) cannot fire. ADR-0014 itself says the clause "lands with
  counts (ii) and (iii)" — count (iii) IS this walk.
- **Step 10** — new delta **MA-12**: KMI §7.1(f) requires three distinguishable answers and neither
  KCB §4's `fetch` ("CAS GET by asset id", no response vocabulary) nor KMI §7 defines a carrier;
  KCB cites §7.1(f) nowhere, and §4.2f independently calls a rate-limited refusal a *pending fetch*,
  so a fourth state shares the word. MA-8's class, one plane over.

Files changed: `scenarios/e2e-multi-authority.md` (+~330 lines), `scenarios/README.md`
(MA-1…MA-11 → MA-1…MA-12), `docs/reference/promotability.md` (two stale links repaired).

### Learnings for future iterations
- **`node scripts/check-doc-integrity.mjs` was RED on this branch before I touched anything** —
  two links to `tasks/chief/87-*.json`, which the retire commit `d73b453` moved to
  `tasks/chief/completed/`. **A retire commit moves a tasklist file and breaks every link to it.**
  Run the guards FIRST each iteration so you know what you inherited.
- The verdict for US-2/US-3 is already determined by this walk and is written into the scenario:
  KINP's prose gate is discharged but KINP does **not** get promoted — the ratification gate
  (`specs/README.md#the-ratification-gate`) also requires a KCS encoding **whose assertions cite the
  clauses being ratified**, and `kcs:multi-authority` predates the fold and asserts none of them
  (DR-8, same shape as DR-7). US-2's first acceptance criterion asserts "nothing else gates it" —
  that premise is contradicted by `promotability.md`'s own KINP row and by the scenario. Follow the
  repo's normative gate, and say so plainly.
- KCB count (iii) and KMI count (i) both stay open. Neither spec is promoted; KCB has four other
  counts and KMI's second count is KCB's work.

## US-2 — KINP is promoted only if its whole gate flipped (2026-09-03)

**Outcome: KINP is NOT promoted.** Status stays **Candidate**, version stays **0.4.0**.

US-2's criterion 1 is conditional and its antecedent *is* satisfied — Steps 2/3/4/6 all flip and
Step 1 holds — but its consequent rests on the premise "KINP has no second count and no downstream
deliverable, so nothing else gates it", and that premise is **false against this repo's own
normative gate**. `specs/README.md` § *The ratification gate* makes a hand-walked prose pass
*necessary but no longer sufficient* for `candidate → ratified`; the second condition is a
machine-replayable KCS encoding **whose assertions cite the clauses being ratified**.
`kcs:multi-authority` predates the 0.4.0 fold and by design asserts none of the folded clauses
(**DR-8**), so §4.1's weakest-link rule, §4.2's `world_aligns_with`, §4.5's fourth branch, §6's
domain-scoping and §3.4's non-federated commons have **no encoded assertion at all**. Criterion 2 is
therefore the one that applies, and it was followed: the status does not move and the blocker is
named in the spec and in `promotability.md`.

Worth noting for whoever reads the tasklist next: `promotability.md`'s **KINP row always said this**
("the **KCS-encoding** condition binds on top of it"). The inaccurate line was the *ranking*
paragraph 5's "What remains is a single re-run and nothing else", which the tasklist description
quoted. Both are now corrected and agree.

Files changed:
- `specs/identity.md` — §11 decision 1's gate paragraph rewritten (records the walk, its per-step
  result, the DEFER-A residual, then the KCS-encoding blocker: downstream under ADR-0001, bounded
  by MA-11 / KCS open question 1, unowned). New **Editorial (2026-09-03)** changelog entry. Fixed a
  stale sentence in the *Downstream evidence* note that still read "MA-1…MA-4 stay blocking and
  unfolded" after 0.4.0 folded them. `Last updated` → 2026-09-03.
- `docs/reference/promotability.md` — KINP blocker cell + owner cell + ranking entry 5; page
  `Updated:` → 2026-09-03.
- `ROADMAP.md` — the stale "KINP's gate is the MA-1…MA-4 fold (`85`)" sentence at Phase 1.
- `CLAUDE.md` — *Current state* KINP bullet (the fourth mirror **no guard checks**).

**Criterion 3 confirmed, not assumed.** Sliced `git show HEAD:specs/identity.md` and the new file on
section headers and compared per section: §0–§10 all `same`, §11 decisions 2 and 3 `same`. Only the
title block (Last updated), §11 decision 1's gate paragraph, one sentence, and the Changelog moved —
**4 diff hunks total**. No relation, envelope field, identifier form or resolution rule changed, so
**no claim id moves**.

**GATES — all green.** `check-doc-integrity` OK (every relative link resolves, all 3 status tables
match their headers) · `check-doc-links` 0 dead references · `check-tasklist-categories` OK, 6
tasklists · `check-schemas` OK · `check-registry` OK · `.chief/verify.sh` → `verify-test: 30 passed,
0 failed`, "✓ verify: all selected gates green".

### Learnings for future iterations
- **A tasklist criterion can carry a false premise, and the repo's normative text outranks it.**
  US-2 criterion 1 asserted "nothing else gates it"; `specs/README.md`'s ratification gate says
  otherwise, and so did `promotability.md`'s own KINP row. The right move was to follow the repo,
  do criterion 2's work, and say plainly in the commit and the notes *why* criterion 1 did not fire.
  US-3's criteria look sound — but read the target file before trusting a criterion's premise.
- **The section-slice check is cheap and worth doing every time** a spec is edited without a version
  bump. `git show HEAD:<file>` → split on `^#{1,3} ` → compare per section. It turns "no clause
  moved" from a claim into an observation, which is exactly what CLAUDE.md asks for.
- **`CLAUDE.md`'s *Current state* prose is the fourth mirror and no guard checks it.** Every
  iteration that touches a spec's gate must edit it by hand. Status did not flip here, so the
  grep-for-counts obligation ("six of six remain candidate") was a no-op — but it will not be if a
  future story ever does flip one.
- `docs/reference/promotability.md` states its own rule that it carries **no current-version
  column** deliberately, precisely so it does not become a fifth unchecked mirror. Do not add one.
