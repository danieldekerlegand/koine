# Chief Progress — 89-the-re-run-three-specs-wait-on
Started: Thu Sep  3 00:43:57 CDT 2026
---

## US-1 — The four steps are walked by hand, and the verdict is per-step (2026-09-03)

Walked Steps 1–10 of `scenarios/e2e-multi-authority.md` by hand against the CURRENT folded spec
text (KINP 0.4.0 / KCB 0.5.0 / KMI 0.3.5), reading each cited section rather than replaying the
encoding. New section in the scenario: **"Re-run — Steps 1–10 walked by hand against the folded
text (2026-09-03)"**, inserted before *Re-ratification — what this pass gates*.

Per-step verdict: 1 ✅ holds · 2 ✅ flips · 3 ✅ flips · 4 ✅ flips · 5 🔴 **does not flip** ·
6 ✅ flips · 7 ✅ flips · 8 ✅ holds · 9 ✅ flips · 10 🔴 **does not flip**.

**The two blockers, both verified against the text, not inferred:**
- **Step 5** — ADR-0014 (Accepted 2026-08-26) is *decided but unwritten*: §7.3's deprecated marking
  has no field in KCB §2's manifest or §3's `find` response, and §3.1(d)'s de-dup converse keys on
  `(provider id, (name,version), schema_id)` so a stale + fresh attribution merge into one entry
  with an undefined marking, and §3.1(e) cannot fire. ADR-0014 itself says the clause "lands with
  counts (ii) and (iii)" — count (iii) IS this walk.
- **Step 10** — new delta **MA-12**: KMI §7.1(f) requires three distinguishable answers and neither
  KCB §4's `fetch` ("CAS GET by asset id", no response vocabulary) nor KMI §7 defines a carrier;
  KCB cites §7.1(f) nowhere, and §4.2f independently calls a rate-limited refusal a *pending fetch*,
  so a fourth state shares the word. MA-8's class, one plane over.

Files changed: `scenarios/e2e-multi-authority.md` (+~330 lines), `scenarios/README.md`
(MA-1…MA-11 → MA-1…MA-12), `docs/reference/promotability.md` (two stale links repaired).

### Learnings for future iterations
- **`node scripts/check-doc-integrity.mjs` was RED on this branch before I touched anything** —
  two links to `tasks/chief/87-*.json`, which the retire commit `d73b453` moved to
  `tasks/chief/completed/`. **A retire commit moves a tasklist file and breaks every link to it.**
  Run the guards FIRST each iteration so you know what you inherited.
- The verdict for US-2/US-3 is already determined by this walk and is written into the scenario:
  KINP's prose gate is discharged but KINP does **not** get promoted — the ratification gate
  (`specs/README.md#the-ratification-gate`) also requires a KCS encoding **whose assertions cite the
  clauses being ratified**, and `kcs:multi-authority` predates the fold and asserts none of them
  (DR-8, same shape as DR-7). US-2's first acceptance criterion asserts "nothing else gates it" —
  that premise is contradicted by `promotability.md`'s own KINP row and by the scenario. Follow the
  repo's normative gate, and say so plainly.
- KCB count (iii) and KMI count (i) both stay open. Neither spec is promoted; KCB has four other
  counts and KMI's second count is KCB's work.
