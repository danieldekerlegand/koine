# Chief Progress — 92-kcbs-four-re-runs-and-kmis-wait
Started: Thu Sep  3 02:41:14 CDT 2026
---

## US-1 — The four KCB re-runs, walked against folded text, one verdict each (2026-09-03)

Four independent hand-walks of KCB's counts (i), (ii), (iv), (v) against the CURRENT folded text
(KCB 0.5.0 as published, not the version each count was declared at). **None closes.** Each verdict
lives in its own scenario document, one section per count — the acceptance criterion is explicit that
a combined verdict destroys the five-counts-each-gating-its-own-section structure.

Files changed
- scenarios/e2e-media-transform.md      — new § *Re-run — the KCB legs walked by hand against KCB 0.5.0*
- scenarios/e2e-live-schema-mutation.md — new § *Re-run — Steps 3, 5, 6, 7, 8, 9 and 10 …*
- scenarios/kcb-subscription-firehose.md— new § *Re-run — Steps 1–8 walked by hand against §4.2*
- scenarios/kcb-cross-owner-posture.md  — new § *Re-run — Steps 1–8 walked by hand against §4.3*
- specs/capability-bus.md               — one *Pressure test* paragraph + one changelog entry. NO version, NO clause.
- scenarios/README.md                   — one consolidated paragraph (it is the register of record)
- CLAUDE.md                             — *Current state* KCB bullet (the fourth mirror; no guard checks it)

Verdicts
- (i)  e2e-media-transform  — F/G/J/K/L all hold against the card extension; the 0.4.2/0.4.3/0.4.7/
       0.4.8/0.5.0 folds confirmed additive **by execution**. NOT clean → **MT-1** (High): §3's path
       plan names no `(name, version)` per leg and §4.4c(2) resolves a version-free invoke to the
       *granted* major, so a caller that planned over the top-ranked successor and holds a predecessor
       grant is served the predecessor **silently**.
- (ii) e2e-live-schema-mutation — V-1/V-2/V-4/V-5/V-7 all flip (V-2/V-4/V-5/V-7 individually
       re-checked, per the acceptance criterion). NOT clean → **V-9** (payload_schema_id is not
       consumer-verifiable), **V-11** (the §7.1 step 5 rule id is MAY on the branch that needs a MUST),
       **V-10** (§7.2's binding row routes a MUST to a frame §7.3g does not name). Plus: **ADR-0014
       reproduces with a SINGLE registry** — it is a base carrier gap, not a federation one, so it
       blocks count (ii) as well as (iii). Plus a bookkeeping correction: the published step list
       mis-files Step 6 exactly as *Fold status* found it mis-filing Step 3.
- (iv) kcb-subscription-firehose — blocking **BP-5 does not reproduce** (§8's parking sentence struck,
       no clause assigns the host anything); BP-1/BP-2/BP-4 do not either. NOT clean → **BP-7**
       (§4.2b's honour-or-refuse rule is stated at *registration* only; §4.2c's *silence is not one of
       them* stops one paragraph short of a live adjustment) and **BP-8**.
- (v)  kcb-cross-owner-posture — **eight of eight AP deltas flip**, including blocking AP-5. NOT clean
       → **AP-9** (Med, carrier). ADR-0013's **W3** restated and unmoved.

Gates: all five guards green; `git diff --name-only main | .chief/verify.sh --run` → verify-test
30 passed / 0 failed, "all selected gates green".

## Learnings for future iterations

- **The single sharpest analytic move in this repo is: find a clause that states a normative
  consequence and then check whether ANY field, frame or table row carries it.** Four of this walk's
  six findings are that shape (V-10, BP-8, AP-9, ADR-0014's marking), and ADR-0014 had already named
  the axis in advance — *every operand kept outside the `schema_id` digest is outside the merge key
  too*. When walking a KCB fold, enumerate the operands outside the digest (`cost`, `volume`,
  `effect`, `binding`, the deprecated marking) and check each against §7.2's table and §7.3g's frame
  table. `cost` and `binding` have rows; `volume` and `effect` do not.
- **A fold's own MAY/MUST asymmetry is where a folded delta comes back.** V-11 and BP-7 are both
  "the section states the discipline correctly one paragraph away and does not extend it": §7.1 step 5
  has a MUST NOT on the branch that doesn't need the rule id and no MUST on the branch that does;
  §4.2c says *silence is not one of them* for `resume` and §4.2b/d say nothing for a live adjustment.
  Read a folded section for which branch got the MUST.
- **Check the published flip/regression step lists before walking them.** `e2e-live-schema-mutation`'s
  *Fold status* corrected Step 3's mis-filing and made the identical error for Step 6 in the same
  breath. A count's step list is prose and is not guarded.
- **`.chief/verify.sh` with no args reports "no changed files" under an agent shell** (its `--plan`
  reads STDIN only when stdin is not a tty, which it never is here). Run it as
  `git diff --name-only main | .chief/verify.sh --run` — or pass the list with `xargs`.
- The five guards are: check-doc-integrity, check-doc-links (`--ratchet --base main`),
  check-tasklist-categories, check-schemas, check-registry. `verify.sh` selects them from changed
  paths and `verify-test.sh` asserts that mapping — the selftest runs as one of the gates.
- **CLAUDE.md's *Current state* prose is the unchecked fourth mirror.** doc-integrity checks three
  status tables against the spec headers and never this. Update it by hand every time.
- Insert new sections with a python `str.index('## Downstream results')` splice — every scenario keeps
  *Downstream results* last, so that marker is a stable insertion point ahead of it.
