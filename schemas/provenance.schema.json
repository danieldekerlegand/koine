{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://koine.ecosystem/schemas/provenance.schema.json",
  "title": "Koine shared building blocks",
  "description": "Shared $defs used by every interchange schema — the machine-readable form of the KINP identity.md §7.1 assertion envelope and the KGP grounding-pack.md §7.1 license axis. Field names are contractual: source, source_url, retrieved_at, confidence, license, tier. Do not rename.",
  "$defs": {
    "contractVersion": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "The koine spec version (semver) the emitting side was built against — e.g. KGP `kgp_version`, KFT `kft_version`. Declared per artifact, never a single library-wide constant."
    },
    "provenance": {
      "type": "object",
      "properties": {
        "source": { "type": "string", "minLength": 1, "description": "Acquisition-source id — a KINP namespace (identity.md §3.4) or an external authority, e.g. 'refkb', 'wikidata', 'analyzer', 'worldsim'." },
        "source_url": { "type": "string" },
        "retrieved_at": { "type": "string", "description": "ISO-8601 UTC." },
        "confidence": { "type": "number", "minimum": 0, "maximum": 1 }
      },
      "required": ["source", "confidence"],
      "additionalProperties": true
    },
    "license": {
      "type": "string",
      "minLength": 1,
      "description": "License class per KGP §7.1 (the admission allowlist axis): an SPDX id (e.g. CC0-1.0, CC-BY-4.0, CC-BY-SA-4.0) or an ecosystem pseudo-id from policy/license-classes.json (e.g. PROPRIETARY, PERSONAL)."
    },
    "tier": {
      "type": "string",
      "enum": ["curated", "acquired", "synthetic", "personal"],
      "description": "Provenance trust tier per KGP §7 (policy/trust-tiers.json) — the descriptive 'how much to believe a source' axis, separate from the dialect (§5) and egress (§7.2) axes. personal and synthetic are containment-gated."
    },
    "dialect": {
      "type": "string",
      "enum": ["grounding-only", "horn-safe", "full-prolog"],
      "description": "Logic dialect per KGP §5 — what logic a consumer may safely evaluate: grounding-only (ground facts, no rules), horn-safe (definite Horn clauses), full-prolog (unrestricted). Its own axis — never conflated with portability. NOTE local-only is NOT a dialect tier — it is an egress class (KGP §5), see the egress $def. Distinct from the egress (§7.2) and trust-tier (§7) axes."
    },
    "egress": {
      "type": "string",
      "enum": ["exportable", "local-only"],
      "default": "exportable",
      "description": "Egress class per KGP §7.2 — whether a record may cross a project boundary at all: exportable (default) or local-only. Enforced at pack construction (producer filters local-only out) and on import (consumer rejects local-only and reports). Its own axis, separate from the descriptive provenance trust tier (§7) and the dialect (§5) axis."
    },
    "csid": {
      "type": "string",
      "pattern": "^cs:[a-z0-9-]+:[^\\s]+$",
      "description": "Canonical entity id: cs:<type>:<local>, QID-anchored when known (cs:language:Q1860). Structurally the KINP entity CURIE (identity.md §7.1)."
    },
    "claimId": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*:claim:sha256-[0-9a-f]{64}$",
      "description": "The KINP assertion-envelope id (identity.md §7.1): <namespace>:claim:sha256-<hex>, e.g. analyzer:claim:sha256-9f3c…. Content-addressed — the hash of the normalized claim (KGP §3), so identical claims from different producers dedup/merge — never a producer-local ad-hoc fact id."
    },
    "assetId": {
      "type": "string",
      "pattern": "^sha256:[0-9a-f]{64}$",
      "description": "Content-addressed asset id — a KMI asset reference (media-interchange.md §2)."
    },
    "nodeRef": {
      "anyOf": [ { "$ref": "#/$defs/csid" }, { "$ref": "#/$defs/assetId" } ],
      "description": "A graph node reference: either a KINP entity CURIE (csid) or a content-addressed KMI assetId."
    }
  }
}
