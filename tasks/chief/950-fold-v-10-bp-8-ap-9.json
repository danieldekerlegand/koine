{
  "project": "koine",
  "category": "unblock",
  "branchName": "chief/950-fold-v-10-bp-8-ap-9",
  "dependsOn": [
    "940-fold-v-9-and-v-11"
  ],
  "touches": [
    "specs",
    "scenarios",
    "docs"
  ],
  "iters": 8,
  "warmup": [
    "sed -n '/^### 7.2 The subscriber-compatibility rule/,/^### 7.3/p' specs/capability-bus.md",
    "sed -n '/^\\*\\*g. A live subscriber is told/,/^### 7.4/p' specs/capability-bus.md",
    "grep -n 'volume\\|effect' specs/capability-bus.md | sed -n '1,40p'",
    "sed -n '/^## Decision/,/^---/p' decisions/ADR-0014-federated-merge-merges-attributions.md | tail -20"
  ],
  "description": "Fold V-10 + BP-8 + AP-9 — ONE EDIT, THREE FINDINGS. They are filed as three because three different pressure tests returned them, and they are folded as one because they are one hole: §7.2's normative table does not govern the two operands added after it was written, and the one clause that tries to route around the gap points at frames that do not exist.\n\nVERIFIED AGAINST THE TEXT (2026-09-12, before this tasklist was authored).\n\nHALF ONE — THE TABLE HAS NO ROW (V-10 / BP-8 / AP-9). §7.2's normative table has thirteen rows and NOT ONE of them is `volume` or `effect`. Both operands nevertheless declare a minor in their own sections: §4.2a says of `volume` that 'changing it is a minor bump on the capability that carries it (§7.2) — the version moves, so a pinned subscriber can see it — exactly as a re-price is', and §4.3a says of `effect` that 'it sits outside the port's `schema_id` digest exactly as `cost` does … is a minor bump on the capability that carries it (§7.2), so the version moves and a pinned consumer sees it'. So two sections declare a bump that the table governing bumps does not authorize — and the nearest row that would reach them ('Editorial only — `description`, examples; no `schema_id` change') reads PATCH. ADR-0014 names this exposure explicitly and declines to close it: 'no bump row for `volume` or `effect` — the exposure is recorded above, and closing it is a §7.2 change that belongs to whichever pressure test breaks it.' Three pressure tests have now broken it.\n\nHALF TWO — A MUST ROUTED TO FRAMES THAT DO NOT EXIST (V-10). §7.2's `binding` row reads: 'Change a capability's transport `binding` (§2.4) | minor | No — but a live `subscribe` MUST be told on §4.2d's channel (§7.3g), never left to a failed dial.' §2.4 routes the same way: 'A live `subscribe` learns of it on §4.2d's control channel, alongside the §7.3g signals, and never by a failed dial.' But §7.3g names exactly THREE frames — `successor_published`, `deprecated`, `removal` — and NONE of them announces a binding change. Two clauses state a MUST and point at a frame table that cannot carry it.\n\nWHY ALL THREE ARE THE SAME AXIS. ADR-0014 named it and tabulated it: an operand kept OUTSIDE the `schema_id` digest, with a declared consequence, and nothing carrying it. `cost` and `binding` are rescued by the `version` their declared bump moves; `volume`, `effect` and the deprecation marking are not rescued at all, because no bump row moves a version for them. §7.1 step 1 confirms the digest half from the other side: `description`, `cost`, `volume` (§4.2a), `effect` (§4.3a), `version` and `binding` are all dropped before hashing, by name.\n\nTHE FOLD. Additive, KCB-only. Give §7.2's table the rows the two operands' own sections already declare, and give §7.3g's frame table whatever the `binding` MUST needs — a fourth frame, or a re-routing of that MUST onto a mechanism that exists. It gates counts (ii), (iv) AND (v), which is why it is one tasklist and not three: three separate edits to §7.2 would collide, and three separate re-runs of three counts over one hole would each be re-reading the other two's absence.",
  "userStories": [
    {
      "id": "US-1",
      "title": "§7.2's table governs `volume` and `effect`",
      "description": "Two sections declare a bump the table does not authorize. Put the rows in the table that governs bumps, so the declaration and the authority agree.",
      "acceptanceCriteria": [
        "§7.2's normative table gains a row for a change to `volume` (§4.2a) and a row for a change to `effect` (§4.3a), each stating the bump and whether it breaks a live subscriber, and each agreeing with what §4.2a and §4.3a already declare in their own text — a minor, so the version moves and a pinned subscriber can see it.",
        "The disagreement the fold removes is stated on the record: before this edit the nearest applicable row read PATCH, and ADR-0014 had named the exposure and deferred it to 'whichever pressure test breaks it'. Three did — V-10, BP-8 and AP-9.",
        "The merge-key consequence is followed through to §3.1(d): an operand rescued by a declared bump is rescued by the `version` the bump moves, not by the key, and the text says which of `cost`, `binding`, `volume`, `effect` and the deprecation marking are now rescued and which are not.",
        "§7.1 step 1's key set does NOT change: `volume` and `effect` stay outside the `schema_id` digest, so no published digest moves. Verified against step 1's explicit drop list, not assumed.",
        "GATES: `node scripts/check-doc-integrity.mjs`, `node scripts/check-doc-links.mjs`, `node scripts/check-schemas.mjs`, `node scripts/check-registry.mjs`, `node scripts/check-tasklist-categories.mjs` and `bash scripts/check-roadmap-truth.sh` all green, with the observed output recorded."
      ],
      "passes": true,
      "notes": "§7.2's table gains a `volume` row (§4.2a) and an `effect` row (§4.3a), both **minor**, both agreeing with what those sections already declared; the removed disagreement is recorded in §7.2 (nearest row read patch; ADR-0014 deferred it to 'whichever pressure test breaks it'; BP-8/AP-9/V-10 broke it). Merge-key consequence followed through to §3.1(d): four of ADR-0014's five carriers (cost, binding, volume, effect) are rescued by the version their bump moves, the deprecation marking is not and cannot be. §7.1 step 1's drop list verified byte-unchanged (git diff shows no line in step 1 touched) — no published digest moves. KCB 0.5.3 -> 0.5.4 with all three mirrors moved. GATES: all six green — check-doc-integrity 0 (61 md files, 1802 links, 3 status tables match), check-doc-links 0 dead vs 0 on main, check-schemas 0, check-registry 0, check-tasklist-categories 0, check-roadmap-truth 0 (also fixed a pre-existing FAIL inherited from main: ROADMAP.md did not cite `940-fold-v-9-and-v-11` by full stem)."
    },
    {
      "id": "US-2",
      "title": "The binding MUST points at a frame that exists",
      "description": "§7.2's `binding` row and §2.4 both route a MUST to §4.2d's channel citing §7.3g, and §7.3g's three frames do not include this one.",
      "acceptanceCriteria": [
        "The MUST that §7.2's `binding` row and §2.4 both state is carried by a mechanism that EXISTS: either §7.3g's frame table gains the frame that announces a changed `binding`, or the MUST is re-routed onto a named mechanism that already carries it. A dangling citation is not an acceptable outcome, and neither is deleting the MUST.",
        "If a frame is added it obeys §7.3g's own rules: it rides §4.2d's EXISTING in-band control channel and mints no second signalling mechanism, it PRECEDES the fact it announces, a subscriber that does not understand it ignores it (§4.2d), and a producer that emits none is detectable rather than merely silent so a scenario can assert its absence (KCS §5).",
        "§2.4's own bounded-on-purpose paragraph is preserved: no provider is required to serve two majors at two endpoints, no naming convention for transport ids is defined, `binding` does not become a discovery key, and §7.1's ban on version-in-the-name is untouched.",
        "§7.3g's stated boundary is unchanged: a discovery binding with no stream open, and a grant (§5), still have no channel, and DEFER-D (a cadence or TTL) stays deferred with its trigger intact.",
        "The KCB version move is decided under §7.2's own table — now including the rows this tasklist added — and recorded with reasoning; if it moves, all three status mirrors (`README.md`, `specs/README.md`, `ECOSYSTEM.md`) move with it, and §2.3's legacy namespace-root removal declared at KCB 0.6.0 is checked rather than tripped.",
        "GATES: koine's six checks green, with observed output recorded."
      ],
      "passes": true,
      "notes": "§7.3g's frame table gains a fourth frame, `entry_changed`, carrying the capability's new `version` and naming which declared non-shape operand moved (`binding` §2.4, `volume` §4.2a, `effect` §4.3a) — one generic frame, the shape the break-test's own fix column proposed. The MUST is carried, not deleted: §2.4, §4.2a, §4.3a and §7.2's three rows all now name the frame. §7.3g's own rules obeyed and verified against the section text — rides §4.2d's existing channel (no second mechanism), PRECEDES the fact (a producer moving a bound `binding` without a preceding frame is now non-conformant, parallel to `removal`), ignored if not understood (§4.2d), and named so KCS §5 can assert its absence. Bounded: MUST NOT announce a shape change (that is `successor_published`), MUST NOT stand in for `deprecated`; `cost` is a MAY since §5 states no telling obligation. §2.4's *bounded on purpose* paragraph is byte-unchanged (no two-endpoint requirement, no transport-id naming convention, `binding` not a discovery key, §7.1's ban untouched) and §7.3g's DEFER-D boundary bullet is byte-unchanged with its trigger intact. The *never left to a failed dial* promise is QUALIFIED rather than deleted — met for the stream holder (which re-dials on reconnect or `invoke`), not met for a cached discovery binding, which is DEFER-D. Version decided and the premise corrected on the record: §7.2's table governs a published CAPABILITY's bumps and decides nothing about KCB's own spec version (§7.3b's axis does); consulted for the two questions it does answer — no published digest moves (a frame is not a card field; §7.1 step 1 byte-unchanged), no live subscriber breaks (§4.2d ignores unknown frames) — so KCB 0.5.4 -> 0.5.5 PATCH, all three mirrors moved (README.md, specs/README.md x2, ECOSYSTEM.md), and §2.3's 0.6.0 removal checked and NOT tripped. GATES: all six green — check-doc-integrity 0 (61 md files, 1803 links, 3 status tables match), check-doc-links 0 dead vs 0 on main, check-schemas 0, check-registry 0, check-tasklist-categories 0, check-roadmap-truth 0; `chief verify` exit 0, all selected gates green."
    },
    {
      "id": "US-3",
      "title": "Three counts, three verdicts — (ii), (iv) and (v), each gating only its own section",
      "description": "One edit, three pressure tests. Each count gets its own recorded verdict; a combined verdict destroys the structure the five counts exist to keep.",
      "acceptanceCriteria": [
        "The three legs are re-run BY HAND against the folded text for the steps their findings were on — `e2e-live-schema-mutation.md` (V-10, count (ii)), `kcb-subscription-firehose.md` (BP-8, count (iv)) and `kcb-cross-owner-posture.md` (AP-9, count (v)) — with a SEPARATE recorded verdict per count, each against the section it gates.",
        "Count (ii)'s walk is made against text that also carries `940`'s V-9/V-11 fold, which is why this tasklist depends on it: a count (ii) re-run over half the findings of one re-run cannot close it.",
        "Every walk is against the PROSE. DR-7 records `kcs:live-schema-mutation` returning `green` over four blocking deltas, and that encoding still predates the 0.5.0 fold — an exit code is not a verdict here.",
        "Verdicts are recorded honestly including NOT-clean ones and any new deltas, and the record names what still blocks each of the three counts. Fold dispositions for V-10, BP-8 and AP-9 are added to the fold-disposition reference, reasoning why one edit answers three findings.",
        "`docs/reference/promotability.md` is updated to match what these walks changed, and states plainly that KCB is not promoted by this tasklist even on three clean re-runs, naming the conditions that remain.",
        "GATES: koine's six checks green, with observed output recorded."
      ],
      "passes": true,
      "notes": "Three counts, three separate verdicts, one per count — NEVER a combined one. All three against the PROSE: the KCS encodings of all three legs are green and now predate this fold (DR-7's shape on three counts at once; kcs:live-schema-mutation now predates FOUR publications of the spec it gates). V-10, BP-8 and AP-9 ALL FAIL TO REPRODUCE, each re-attacked on the ground it was filed on. NONE OF THE THREE COUNTS CLOSES. (ii) e2e-live-schema-mutation.md — Steps 2, 3, 7, 10 walked (2/3 because the table moved under them, 7 because §2.4's bullet moved, 10 because V-10 was filed there); Steps 5/6/9/11 restated unmoved on byte-unchanged clauses so V-12/V-13/V-14/V-15 stand as filed. New V-16 (Med-High, payload): the frame MUST carry the new `version` and MUST name which operand moved, and carries NO NEW VALUE — so §7.3g's 'puts the new address in its hands' and §2.4's 'dials the new address' follow only from a re-describe no clause requires, and `successor_published` carries a `binding` where this frame does not. (v) kcb-cross-owner-posture.md — Step 6. AP-9 does not reproduce on either leg; new AP-10 (Med-High), the same defect on the `effect` axis and sharper, because §4.3f evaluates a subscribe posture ONCE at registration so §4.3c's intersection has no class and §4.3d's floor has no evaluation point on a live stream. §4.3 needs no change. W3 unmoved. (iv) kcb-subscription-firehose.md — Step 8, Step 1 re-checked. BP-8 flips; NO new delta — the same omission was put to `volume` and does not bite (three recorded reasons), a decision not an omission. Held open by BP-7 alone. Step 8's own wording corrected: the hold '§7.2's table is not disturbed' is now literally false and the property it asserted (no live subscriber breaks) is what holds, both new rows reading No. Fold dispositions for V-10/BP-8/AP-9 added to capability-versioning-fold-dispositions.md (one hole, two halves tabulated, why one generic frame beat three specific ones, why two table rows are not a new bump tier), plus three rows in 'Where each fold lands'. promotability.md item (4) rewritten to V-16 + AP-10 with the KCB row, the ownership paragraph and the re-derived ranking all updated, and it states plainly that this tasklist promotes nothing: counts (ii)/(iv)/(v) each changed shape, count (iii)'s MA-14/15/16, MT-1, DR-7 and W3 stand regardless. NO VERSION AND NO CLAUSE MOVED — specs/capability-bus.md diff is 127 insertions, 0 DELETIONS (four gate/pressure paragraphs + one Editorial changelog entry); every normative clause of §1–§8 byte-unchanged, §7.3g's four rows and §7.2's table included, no published digest moves. GATES: all six green — check-doc-integrity 0 (61 md files, 1819 links, 3 status tables match), check-doc-links 0 dead vs 0 on main, check-schemas 0, check-registry 0, check-tasklist-categories 0 (5 categorized), check-roadmap-truth 0 (coverage 57 completed, stale 47 merged, park OK); `chief verify` exit 0, all selected gates green, verdict RECORDED."
    }
  ]
}
