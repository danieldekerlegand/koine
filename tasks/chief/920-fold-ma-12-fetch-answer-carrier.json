{
  "project": "koine",
  "category": "unblock",
  "branchName": "chief/920-fold-ma-12-fetch-answer-carrier",
  "dependsOn": [],
  "touches": [
    "specs",
    "scenarios",
    "docs"
  ],
  "iters": 8,
  "warmup": [
    "sed -n '/Absence must be answerable/,/DEFER-C/p' specs/media-interchange.md",
    "sed -n '/^## 4. Verbs/,/^### 4.1/p' specs/capability-bus.md",
    "sed -n '/^\\*\\*f. Composition across a fetch fan-out/,/^\\*\\*g\\./p' specs/capability-bus.md",
    "grep -n 'MA-12' scenarios/e2e-multi-authority.md docs/reference/*.md | head -20"
  ],
  "description": "Fold MA-12 — the blocker the 2026-09-03 re-run left on KMI count (i), and the half whose carrier belongs on KCB §4's verb.\n\nVERIFIED AGAINST THE TEXT (2026-09-12, before this tasklist was authored). Every leg of the row's claim holds:\n\n1. KMI §7.1(f)'s MA-10 paragraph requires the three-valued answer: a store MUST be able to answer, for an id it is asked for, 'not held, and not expected' — it holds no copy and has no replication of that id in flight or scheduled — DISTINCTLY FROM 'not reachable' and from 'not held, pending'.\n2. NOTHING CARRIES IT. KCB §4's verb table defines `fetch` as 'CAS GET by `asset` id; integrity self-verifies against the hash (delta G). Requires a `fetch:asset` grant (§5)' — an address and a grant, and no response vocabulary at all.\n3. KMI §7 defines the payloads, not the pipe, and mints no field, enum or envelope for any of the three answers.\n4. KCB does not cite §7.1(f). The one KCB clause that reaches for KMI §7.1 is §4.2f, which cites it for replication-on-reference placement ('the limit is applied by the participant that holds the bytes, in its own authority domain') and not for the three answers.\n5. §4.2f ALREADY SPENDS THE FOURTH STATE: 'a refused `fetch` is a pending fetch, which delta L's dangling-reference tolerance already requires every consumer to handle.' So a rate-limited refusal and a not-yet-propagated reference are the same word today, and *pending* is exactly the reading a fold must not let absence collapse into.\n\nTHE FOLD, AND ITS SHAPE — CONSTRAINED BY THE 2026-09-03 WALK. Two specs, KCB §4 + KMI §7.1(f): give the three answers a carrier ON THE VERB THAT DELIVERS THEM, with KCB §4 citing KMI §7.1(f) as the clause that defines their meaning. The Step 10 re-attack of 2026-09-03 constrained it further and those constraints are not optional: the response vocabulary is NAMED, it is owed PER REQUEST, and it is never SYNTHESIZED. Absence reads *pending* and NEVER *not expected* — a store that says nothing has not said 'no holder remains'.\n\nADDITIVE. No `asset` id moves, no envelope field is added, a single-store deployment is unaffected, and DEFER-C — the optional designated durable holder — is UNMOVED and keeps its stated trigger.\n\nTHEN RE-RUN STEPS 8-10. KMI's other count is KCB's work (the `e2e-media-transform` re-run), so even a clean re-run leaves KMI Candidate. Say so in the record. MA-13 is the second blocker on the same count and is the next tasklist; the re-run that closes count (i) must clear both.",
  "userStories": [
    {
      "id": "US-1",
      "title": "A named response vocabulary on KCB §4's fetch, owed per request and never synthesized",
      "description": "The carrier half. §4's `fetch` is a CAS GET with no response vocabulary; give it one, for the three answers KMI §7.1(f) already requires a store to distinguish.",
      "acceptanceCriteria": [
        "KCB §4's `fetch` gains a NAMED response vocabulary for the three answers KMI §7.1(f) requires — *not held, and not expected* / *not reachable* / *not held, pending* — with §4 citing KMI §7.1(f) as the clause that defines their meaning rather than restating it.",
        "The three constraints the 2026-09-03 Step 10 re-attack imposed all hold in the text: the vocabulary is NAMED (not left to an implementer's status string), it is owed PER REQUEST, and a value for it is NEVER synthesized by a party that did not determine it.",
        "ABSENCE READS *pending*, and never *not expected*. A store that answers nothing, or that a consumer cannot reach, has not asserted that no holder remains — §7.1(f)'s own conclusion rule (a consumer MAY conclude only for the set it reached) is preserved word for word.",
        "§4.2f's existing *pending fetch* for a rate-limited refusal is reconciled rather than contradicted: the text says which of the answers a refused `fetch` is, so the fourth state §4.2f already spent is not two things at once.",
        "Additive: no verb is added (§4's table still has exactly five entries), no `asset` id moves, no envelope field is added, and a single-store deployment behaves exactly as it did before. Verified against the text, not asserted.",
        "GATES: `node scripts/check-doc-integrity.mjs`, `node scripts/check-doc-links.mjs`, `node scripts/check-schemas.mjs`, `node scripts/check-registry.mjs`, `node scripts/check-tasklist-categories.mjs` and `bash scripts/check-roadmap-truth.sh` all green, with the observed output recorded."
      ],
      "passes": true,
      "notes": "KCB 0.5.2. New §4.5 names four outcomes on §4's `fetch` (`held` / `not-held-pending` / `not-held-not-expected` / `refused`), owed per request, never synthesized, absence reading *pending*; §7.1(f) cited for the meanings, conclusion rule preserved word for word; §4.2f reconciled (its *pending fetch* is the consumer's handling, and a rate-limited refusal MUST answer `refused`). Additivity verified against the text by section slice, not asserted: §1-§3 and §5-§8 byte-unchanged, §4's verb table still has exactly 5 entries (counted), no `asset` id / envelope field / `schema_id` digest moves. Gates: chief verify exit 0, all selected gates green; run by hand, doc-integrity OK (61 files, 1722 links, all 3 status tables match), doc-links 0 dead vs 0 on main, schemas OK (9 schemas, 278 positions, 80 $refs, 3 fixtures), registry OK (31 relation names), tasklist-categories OK (8 categorized), roadmap-truth OK (coverage 54, stale 44, park clean)."
    },
    {
      "id": "US-2",
      "title": "KMI §7.1(f) points at the carrier, and DEFER-C stays deferred",
      "description": "The KMI half is small on purpose: §7.1(f) states the obligation already, and what it lacked was a wire. Point it at the wire and change nothing else.",
      "acceptanceCriteria": [
        "KMI §7.1(f) names KCB §4's response vocabulary as where the three answers are carried, without restating the vocabulary or minting a KMI field for it — §7 defines the payloads and not the pipe, and this fold must not reverse that.",
        "DEFER-C is explicitly UNMOVED: no minimum replica count, no retention obligation, no durability guarantee, and no designated durable holder. Its trigger in `docs/reference/federation-fold-dispositions.md` is unchanged.",
        "The version moves for BOTH specs are decided under each spec's own rules and recorded with reasoning; if either moves, all three status mirrors (`README.md`, `specs/README.md`, `ECOSYSTEM.md`) move with it, which `check-doc-integrity.mjs` enforces. KMI's declared removal of `application/vnd.koine.edl+json` at KMI 0.4.0 and KCB's legacy namespace-root removal at KCB 0.6.0 are checked rather than tripped by whatever bump this takes.",
        "A fold disposition is recorded for MA-12 in the fold-disposition reference alongside the MA-1…MA-11 dispositions, reasoning the fold's EXTENT — what the break forces and what it does not.",
        "GATES: koine's six checks green, with observed output recorded."
      ],
      "passes": true,
      "notes": "KMI 0.3.6 -> 0.3.7 (patch). §7.1(f) gains two paragraphs naming KCB §4.5 as the carrier and nothing more: KMI mints no field/enum/envelope (§7 defines the payloads, not the pipe), keeps the MEANINGS (§4.5 cites this clause; on disagreement about a meaning §7.1(f) governs), and states the three consequences of the split (owed per request; never synthesized; silence is *not reachable*, the absence of an answer). Conclusion rule unchanged in either direction. DEFER-C explicitly unmoved in the status note, the changelog, the §7.1 count paragraph and CLAUDE.md; its trigger row in federation-fold-dispositions.md is byte-unchanged (the only removed line in that file is the Updated: date). Version reasoning recorded on both planes: KCB 0.5.2 and KMI 0.3.7 are patches under each spec's own rules, KMI 0.4.0 stays spent on the EDL removal and KCB 0.6.0 stays spoken for by §2.3's removal — checked, not tripped. All three status mirrors moved (README.md, specs/README.md, ECOSYSTEM.md) plus the un-gated prose list at specs/README.md:288. MA-12's disposition added to the fold-disposition reference beside MA-1…MA-11, reasoning extent: a carrier is forced, an obligation to HOLD is not (DEFER-C's case), and which plane each half lands on is the define-once rule applied in reverse. Additivity proven by section slice, not asserted: §1-§6, §8, §9 and Pressure test byte-unchanged; only §7, Changelog and the header differ. GATES green: chief verify exit 0 RECORDED (tree b6720629); by hand doc-integrity OK (61 files, 1732 links, all 3 status tables match, media-interchange 0.3.7 candidate), doc-links 0 dead vs 0 on main, schemas OK (9 schemas, 278 positions, 80 $refs, 3 fixtures — none touched), registry OK (31 relation names), tasklist-categories OK (8 categorized), roadmap-truth OK (coverage 54, stale 44, park clean)."
    },
    {
      "id": "US-3",
      "title": "Re-run Steps 8-10, and record that KMI stays Candidate either way",
      "description": "The re-run is the point of the fold. Walk the prose; a green encoding is not the verdict here (DR-8).",
      "acceptanceCriteria": [
        "`scenarios/e2e-multi-authority.md` Steps 8-10 are re-run BY HAND against the folded text, with a per-step verdict recorded in the scenario. Step 10 is the step MA-12 was found on and re-confirmed on, so it is the step the walk must answer.",
        "The walk is against the PROSE: DR-8 records `kcs:multi-authority` returning `green` over six blocking deltas, so the encoding's exit code is not evidence.",
        "The verdict is recorded honestly, including a NOT-clean one. Count (i) does not close while MA-13 stands — say which blockers remain rather than implying the count moved.",
        "The record states that KMI's other count is the `e2e-media-transform` re-run, which is KCB's work, so KMI stays Candidate even on a clean walk. `docs/reference/promotability.md` is updated to match.",
        "GATES: koine's six checks green, with observed output recorded."
      ],
      "passes": true,
      "notes": "Steps 8-10 re-run BY HAND against KMI 0.3.7 / KCB 0.5.2 (prose, not a replay — DR-8: kcs:multi-authority returned green over six blocking deltas and now predates three folds). Verdicts: Step 8 HOLDS (third confirmation — the digest axis put to §7.1(a) again; (a)/(c)/(d) byte-unchanged), Step 9 DOES NOT FLIP (MA-13 stands, unfolded — §7.1(d)(e) verbatim; §4.5 correctly gives it no route to the missing operand), Step 10 DOES NOT FLIP. MA-12 DOES NOT reproduce — §4.5(a)-(e) attacked clause by clause, none yielded, §7.1(f)'s conclusion rule preserved word for word — but two NEW deltas: MA-17 (High, carrier — §4.5(a) forbids inferring an outcome and then fixes no field/key/header for it, on the one verb §4 types by no protocol, §4.1 audits as 'not an MCP call at all' and §4.5 says is not typed by a port; the private status string §7.1(f) forbids returns AS THE SLOT, and (c)'s absence-reads-pending makes the failure silent; reproduces with one store and one authority) and MA-18 (Med, mis-route — (a)'s SHOULD routes a fetch refusal's why to §4.3h, whose MUSTs name §4.3 and a posture class, both excluded for fetch by §4.3a/§4.3f). One additive KCB-only §4.5(a) edit for both, unowned. NEITHER count closes: KMI count (i) takes a fifth shape (fold MA-13 + MA-17 + MA-18, then re-run), KCB count (vi) reads fold MA-17 + MA-18. Recorded honestly as not-clean. KMI stays Candidate either way and promotability.md says so: count (ii) is KCB's work (MT-1) and the conformance gate fails on DR-4 and DR-8/DR-7 — no result of Steps 8-10 could have promoted it. promotability.md updated: KMI row, KCB row (five counts -> six), register seven -> eight things + item (8), ranking rows 5/6, section 5 prose, and a dated ownership note correcting the 'unowned' column (queued tasklists 940/950/960/970 name items 2-5; MA-13 is queued as 930). No version and no clause moved — proven by section slice: KCB §1-§3, §5-§8 and Pressure test 'same', KMI §1-§6, §8, §9 and Pressure test 'same'; the only removed lines in either spec are two status-note lines I replaced. GATES green: chief verify exit 0 RECORDED (tree b72e8c14); by hand doc-integrity OK (61 files, 1741 links, all 3 status tables match), doc-links 0 dead vs 0 on main, schemas OK (9 schemas, 278 positions, 80 $refs, 3 fixtures — none touched), registry OK (31 relation names), tasklist-categories OK (8 categorized), roadmap-truth OK (coverage 54, stale 44, park clean)."
    }
  ]
}
