{
  "project": "koine",
  "category": "unblock",
  "branchName": "chief/960-fold-mt-1-version-per-plan-leg",
  "dependsOn": [],
  "touches": [
    "specs",
    "scenarios",
    "docs"
  ],
  "iters": 7,
  "warmup": [
    "sed -n '/^## 3. Discovery registry/,/^### 3.1/p' specs/capability-bus.md",
    "sed -n '/^\\*\\*c. Resolution/,/^\\*\\*d. The quoted cost/p' specs/capability-bus.md",
    "grep -n 'MT-1' scenarios/e2e-media-transform.md ROADMAP.md docs/reference/*.md | head",
    "grep -n 'Composition\\|path' specs/capability-bus.md | sed -n '1,25p'"
  ],
  "description": "Fold MT-1 \u2014 the finding the 2026-09-03 re-run of `e2e-media-transform.md` returned, and the blocker on KCB count (i).\n\nVERIFIED AGAINST THE TEXT (2026-09-12, before this tasklist was authored).\n\n\u00a73'S PATH PLAN NAMES NO VERSION PER LEG. \u00a73's *Composition* bullet has the registry compute 'a *path* from a start port to a goal port across planes and providers \u2026 resolved by matching the ports crawled off peers' card extensions', preferring zero-`cost` routes and returning the path's projected cost. It names ports, planes, providers and cost \u2014 and no `(name, version)` for any leg. \u00a73's *Ranking across versions* bullet does fix that the registry ranks the HIGHEST SATISFYING VERSION first, so the plan is BUILT over a specific version even though it does not SAY which.\n\n\u00a74.4c(2) THEN RESOLVES SOMEWHERE ELSE, SILENTLY. \u00a74.4c's normative order is: (1) the `version` operand where present; (2) otherwise THE GRANT'S MAJOR, where readable; (3) otherwise, where more than one major is published, REFUSE for want of a version; (4) otherwise the one published major. A caller that planned over the top-ranked successor and holds a predecessor grant carries no `version` operand, so case (2) fires and it is served the PREDECESSOR \u2014 silently, having planned over the successor, and having been quoted the successor's cost by \u00a73's own path search.\n\nWHY IT IS THE SYMMETRIC CASE \u00a74.4c ALREADY FORBIDS BY NAME. \u00a74.4c(3) forbids resolving to the highest published major in terms: 'A provider MUST NOT pick, and MUST NOT resolve to the highest published major: that is the fail-open inversion V-5 found, and it is forbidden by name.' MT-1 is the same failure with the sign reversed \u2014 a silent resolution AWAY from what the caller planned over. And it does not disagree with the GRANT, which is correct and binding; it disagrees with \u00a73's own PLAN, which nothing on the wire carries.\n\nTHE FOLD. Additive, KCB-only, two edits: \u00a73 names the version a leg was MATCHED OVER, so a plan says what it planned against; and \u00a74.4c refuses a resolved major that DIFFERS from a presented plan leg \u2014 the same instrument \u00a74.4c and \u00a75 already use, refusing where a value could mean two things and no party is entitled to guess. Nothing here requires a caller to pin, nothing makes the plan binding on the provider, and a consumer content to follow a provider's single published major carries nothing and stays conformant (\u00a74.4e).\n\nTHEN RE-RUN COUNT (i). That re-run is also KMI's second condition, so a clean walk here is worth more than one spec's count \u2014 but it closes only KCB count (i), and KCB's other four counts are untouched.",
  "userStories": [
    {
      "id": "US-1",
      "title": "\u00a73's path plan names the version each leg was matched over",
      "description": "The plan is built over a specific version by \u00a73's own ranking rule and says so nowhere. Make it say so.",
      "acceptanceCriteria": [
        "\u00a73's *Composition* bullet (and the path result it describes) names the `(name, version)` each leg was MATCHED OVER, so a plan carries what it planned against \u2014 consistent with \u00a73's *Ranking across versions* rule, which already selects the highest satisfying version.",
        "Additive and consistent with the rest of \u00a73: the registry still returns ADDRESSES and never proxies (ADR-0001), ranking rules do not change, and a single-registry deployment is conformant unchanged. A consumer MUST ignore fields it does not understand (\u00a77.2), so an older consumer is unaffected.",
        "The projected cost \u00a73 already returns is reconciled with the named version, so a caller that gates spend on the plan is gating against the same leg it planned over.",
        "GATES: `node scripts/check-doc-integrity.mjs`, `node scripts/check-doc-links.mjs`, `node scripts/check-schemas.mjs`, `node scripts/check-registry.mjs`, `node scripts/check-tasklist-categories.mjs` and `bash scripts/check-roadmap-truth.sh` all green, with the observed output recorded."
      ],
      "passes": true,
      "notes": "KCB 0.5.6 (patch). \u00a73 gains a NORMATIVE bullet: each returned path leg names the `(name, version)` it was matched over (exact version, never a range, \u00a77.1's `0.0.0`-unknown as a value), the projected cost is the cost of exactly those legs, and a plan leg is the registry's account of what it matched \u2014 ADR-0001's addresses-not-proxy rule untouched, ranking unchanged, single-registry deployments conformant unchanged, \u00a77.2's ignore-unknown-fields covering older consumers. Three status mirrors moved with the header (README.md, specs/README.md incl. its prose line, ECOSYSTEM.md). GATES observed: check-doc-integrity green (61 markdown files, 1823 relative links, all 3 status tables match headers, exit 0); check-doc-links --ratchet --base main green (0 dead vs 0 on main, exit 0); check-schemas green (9 schemas, 278 positions, 80 $refs, 3 fixtures, exit 0); check-registry green (31 relation names unique, exit 0); check-tasklist-categories green (4 tasklists, exit 0); check-roadmap-truth green (coverage 58 completed named, stale 48 checked none open, park OK, exit 0). Six of six green."
    },
    {
      "id": "US-2",
      "title": "\u00a74.4c refuses a resolved major that differs from a presented plan leg",
      "description": "The refusal instrument \u00a74.4c and \u00a75 already use, applied to the one disagreement they do not currently catch.",
      "acceptanceCriteria": [
        "\u00a74.4c refuses an `invoke` whose RESOLVED major differs from the major of a presented plan leg, naming both \u2014 the same instrument \u00a74.4c(3) uses for *want of a version* and \u00a75 uses for an unstated `budget_units` unit, and stated as one rule applied again rather than a new convention.",
        "The grant still binds and is not widened: \u00a74.4c's existing rule that a resolved major outside the granted major is refused at the gate, before the work, is untouched. The new refusal is about the PLAN, which is a different disagreement from the grant.",
        "The fold does not reintroduce what \u00a74.4c forbids: no default is minted, no highest-published fallback appears, and a refusal is a refusal and not a counter-offer (\u00a74.4e) \u2014 a re-dispatch under a different operand is a new `invoke`.",
        "Additive at every surface: presenting a plan leg is OPTIONAL, an `invoke` carrying none of \u00a74.4's operands against a provider publishing one major behaves exactly as it did at 0.4.9, and no verb, plane, port kind or authority role is added.",
        "The KCB version move is decided under \u00a77.2's own table and recorded with reasoning; if it moves, all three status mirrors (`README.md`, `specs/README.md`, `ECOSYSTEM.md`) move with it, and \u00a72.3's legacy namespace-root removal declared at KCB 0.6.0 is checked rather than tripped.",
        "GATES: koine's six checks green, with observed output recorded."
      ],
      "passes": true,
      "notes": "KCB 0.5.7 (patch), the enforcement half of MT-1. \u00a74.4c gains OPTIONAL `planned_leg` (the `(name, version)` \u00a73 named for the leg this call executes) and a MUST: where the RESOLVED major differs from the presented leg's major, refuse *plan mismatch* naming BOTH \u2014 stated as the instrument \u00a75 (MA-6 unit) and \u00a74.4c(3) (want of a version) already share, applied a third time. Grant rule byte-unchanged and named a different party; no default, no highest-published fallback, never an operand of resolution (checked AFTER the order, never a fifth case, never a stand-in for a missing `version`); refusal is not a counter-offer (\u00a74.4e). Comparison is on the major only (`1.4.0` vs major 1 is not a mismatch); a leg naming another capability is refused, never ignored; `0.0.0`-unknown compared like any other, nothing matches leniently. Additive: operand OPTIONAL both directions, an `invoke` with none of \u00a74.4's operands against a one-major provider behaves as at 0.4.9, no verb/plane/port kind/authority role added. Bump decided under \u00a77.3b's axis with \u00a77.2's table consulted for the two questions it answers (no published digest moves \u2014 \u00a77.1 step 1 byte-unchanged; no live subscriber breaks) and \u00a72.3's 0.6.0 removal checked, not tripped; all three mirrors moved (README.md, specs/README.md table + prose line 288, ECOSYSTEM.md). GATES observed: check-doc-integrity green (61 markdown files, 1827 relative links, all 3 status tables match headers, exit 0); check-doc-links --ratchet --base main green (0 dead vs 0 on main, exit 0); check-schemas green (9 schemas, 278 positions, 80 $refs, 3 fixtures, exit 0); check-registry green (31 relation names unique, exit 0); check-tasklist-categories green (4 tasklists, exit 0); check-roadmap-truth green (coverage 58, stale 48 checked none open, park OK, exit 0). Six of six green; `chief verify` exit 0, all selected gates green."
    },
    {
      "id": "US-3",
      "title": "Re-run count (i) and give it a verdict",
      "description": "`e2e-media-transform.md` is the count MT-1 blocks, and its re-run has been outstanding since 2026-07-22 against the 0.3.0 AgentCard-extension shape.",
      "acceptanceCriteria": [
        "`scenarios/e2e-media-transform.md` is re-run BY HAND against the folded text, with a per-step verdict recorded in the scenario, and count (i) gets an explicit verdict \u2014 including a NOT-clean one with any new delta named.",
        "The walk is against the PROSE. `kcs:media-transform` ran green in 2026-08 and DR-4 records `kcs:kmi-otio-roundtrip` being that same scenario re-titled over the same fixture, so a green encoding is not evidence about the folded text.",
        "The record states what a clean count (i) does and does not buy: it is also KMI's second promotion condition, and it closes only KCB count (i) \u2014 KCB's other four counts are untouched, so nothing is promoted here.",
        "A fold disposition for MT-1 is recorded in the fold-disposition reference, reasoning the extent of the fold \u2014 in particular that it disagrees with \u00a73's plan rather than with the grant, and that the grant rule is deliberately unmoved.",
        "`docs/reference/promotability.md` is updated to match what this walk changed.",
        "GATES: koine's six checks green, with observed output recorded."
      ],
      "passes": true,
      "notes": "Count (i) re-run by hand against KCB 0.5.7 / KMI 0.3.8 (the first text carrying both halves of the MT-1 fold), Steps 1, 3, 4 and 8, per-step verdicts recorded in a new `scenarios/e2e-media-transform.md` section. Against the PROSE, never a replay \u2014 that encoding predates eight publications of the spec it gates (DR-7), is the one `kcs:kmi-otio-roundtrip` re-titles over one fixture (DR-4), and answered Steps 3/4's provider half from a `standin` (DR-1). VERDICT: count (i) does NOT close. MT-1 does not reproduce (plan says what it planned over; refusal names both majors; major-only comparison holds under probe; the operand cannot select; grant rule byte-unchanged and a different party), and F/G/J/K/L hold for a second consecutive walk with K stronger by construction. Two new perimeter deltas, both on the fold published hours earlier: MT-2 (High, carrier \u2014 \u00a73's 'it mints no field' is true of a leg and false of a path; no clause of \u00a73, \u00a74 or \u00a77 types a path request or result, and \u00a74.4c types `planned_leg` 'as \u00a73 returned it' against that absence, so an unparseable plan is an absent plan; reproduces with one registry and one provider) and MT-3 (Med-High, scope \u2014 the cross-check binds the party that cannot know a selection happened, and no response names the resolved major). Recorded: what a clean count (i) does and does not buy (it is KMI's second promotion condition, and closes only KCB count (i) \u2014 the other five are restated, nothing is promoted). MT-1 disposition added to `docs/reference/capability-versioning-fold-dispositions.md` (title updated; the extent reasoned, including why the grant rule is deliberately unmoved \u2014 refusing at \u00a74.4c(2) would make a successor unreachable, the failure \u00a74.4 rejects by name from the other side). `docs/reference/promotability.md` updated (KCB row, ranking prose, KMI's count (ii) dependency, and register item (2) now MT-2 + MT-3). ROADMAP.md, scenarios/README.md and CLAUDE.md moved with it. NO version and NO clause moved for the walk: \u00a71-\u00a78 byte-unchanged, no published digest moves. GATES observed: check-doc-integrity green (61 markdown files, 1836 relative links, all 3 status tables match headers, exit 0); check-doc-links --ratchet --base main green (0 dead vs 0 on main, exit 0); check-schemas green (9 schemas, 278 positions, 80 $refs, 3 fixtures, exit 0); check-registry green (31 relation names unique, exit 0); check-tasklist-categories green (4 tasklists, exit 0); check-roadmap-truth green (coverage 58 completed named, stale 48 checked none open, park OK, exit 0). Six of six green; `chief verify` exit 0, all selected gates green."
    }
  ]
}
